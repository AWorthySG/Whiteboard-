diff --git a/node_modules/tldraw/dist-cjs/lib/shapes/draw/getPath.js b/node_modules/tldraw/dist-cjs/lib/shapes/draw/getPath.js
index fcc0144..c9cf62b 100644
--- a/node_modules/tldraw/dist-cjs/lib/shapes/draw/getPath.js
+++ b/node_modules/tldraw/dist-cjs/lib/shapes/draw/getPath.js
@@ -29,22 +29,25 @@ const PEN_EASING = (t) => t * 0.65 + (0, import_editor.SIN)(t * import_editor.PI
 const simulatePressureSettings = (strokeWidth) => {
   return {
     size: strokeWidth,
-    thinning: 0.5,
-    streamline: (0, import_editor.modulate)(strokeWidth, [9, 16], [0.64, 0.74], true),
-    // 0.62 + ((1 + strokeWidth) / 8) * 0.06,
-    smoothing: 0.62,
+    thinning: 0.7,
+    streamline: 0.5,
+    smoothing: 0.55,
     easing: import_editor.EASINGS.easeOutSine,
-    simulatePressure: true
+    simulatePressure: true,
+    start: { taper: 25, cap: true },
+    end: { taper: 25, cap: true }
   };
 };
 const realPressureSettings = (strokeWidth) => {
   return {
     size: 1 + strokeWidth * 1.2,
-    thinning: 0.62,
-    streamline: 0.62,
-    smoothing: 0.62,
+    thinning: 0.82,
+    streamline: 0.4,
+    smoothing: 0.55,
     simulatePressure: false,
-    easing: PEN_EASING
+    easing: PEN_EASING,
+    start: { taper: 30, cap: true },
+    end: { taper: 30, cap: true }
   };
 };
 const solidSettings = (strokeWidth) => {
diff --git a/node_modules/tldraw/dist-cjs/lib/shapes/shared/default-shape-constants.js b/node_modules/tldraw/dist-cjs/lib/shapes/shared/default-shape-constants.js
index c2ba5d9..1a5db30 100644
--- a/node_modules/tldraw/dist-cjs/lib/shapes/shared/default-shape-constants.js
+++ b/node_modules/tldraw/dist-cjs/lib/shapes/shared/default-shape-constants.js
@@ -37,7 +37,7 @@ const TEXT_PROPS = {
   padding: "0px"
 };
 const STROKE_SIZES = {
-  s: 2,
+  s: 1.5,
   m: 3.5,
   l: 5,
   xl: 10
diff --git a/node_modules/tldraw/dist-esm/lib/shapes/draw/getPath.mjs b/node_modules/tldraw/dist-esm/lib/shapes/draw/getPath.mjs
index d1fdab0..4d4833f 100644
--- a/node_modules/tldraw/dist-esm/lib/shapes/draw/getPath.mjs
+++ b/node_modules/tldraw/dist-esm/lib/shapes/draw/getPath.mjs
@@ -9,22 +9,25 @@ const PEN_EASING = (t) => t * 0.65 + SIN(t * PI / 2) * 0.35;
 const simulatePressureSettings = (strokeWidth) => {
   return {
     size: strokeWidth,
-    thinning: 0.5,
-    streamline: modulate(strokeWidth, [9, 16], [0.64, 0.74], true),
-    // 0.62 + ((1 + strokeWidth) / 8) * 0.06,
-    smoothing: 0.62,
+    thinning: 0.7,
+    streamline: 0.5,
+    smoothing: 0.55,
     easing: EASINGS.easeOutSine,
-    simulatePressure: true
+    simulatePressure: true,
+    start: { taper: 25, cap: true },
+    end: { taper: 25, cap: true }
   };
 };
 const realPressureSettings = (strokeWidth) => {
   return {
     size: 1 + strokeWidth * 1.2,
-    thinning: 0.62,
-    streamline: 0.62,
-    smoothing: 0.62,
+    thinning: 0.82,
+    streamline: 0.4,
+    smoothing: 0.55,
     simulatePressure: false,
-    easing: PEN_EASING
+    easing: PEN_EASING,
+    start: { taper: 30, cap: true },
+    end: { taper: 30, cap: true }
   };
 };
 const solidSettings = (strokeWidth) => {
diff --git a/node_modules/tldraw/dist-esm/lib/shapes/shared/default-shape-constants.mjs b/node_modules/tldraw/dist-esm/lib/shapes/shared/default-shape-constants.mjs
index b718510..ae47144 100644
--- a/node_modules/tldraw/dist-esm/lib/shapes/shared/default-shape-constants.mjs
+++ b/node_modules/tldraw/dist-esm/lib/shapes/shared/default-shape-constants.mjs
@@ -6,7 +6,7 @@ const TEXT_PROPS = {
   padding: "0px"
 };
 const STROKE_SIZES = {
-  s: 2,
+  s: 1.5,
   m: 3.5,
   l: 5,
   xl: 10
